node_modules/
.next/
.env.local
# Local secret-file backups — never commit. Covers .env.local.backup, .env.local.bak, etc.
# Intentional .env.local.example stays tracked because it doesn't match these patterns.
.env*.backup
.env*.bak
.vercel
*.tsbuildinfo
.DS_Store

# Importer run logs — generated per run, noisy in the repo
scripts/.import-runs/

# Scratch / cowork apply-spec files — local working artifacts, not for the repo.
# (Mistakenly swept in by `git add -A` in 4861711; untracked in the follow-up.)
/CampaignEditorBanner.tsx
/alphaboost_picker_data.json
/media-library-followups-spec.md
/phase2/
/phase3/
/phase4/
/route.ts
/scripts/walk-alphaboost-folders.js
/walk-stderr.log

# Backfill / script run logs — generated per run
scripts/logs/

# Local-only script variants (not for the repo)
scripts/*.local.js
